# Beauty Zone

## How to run

Open the project in VS Code and click `Go Live` in the bottom status bar.

## SCSS

Edit styles in the `scss` folder. The browser reads the compiled CSS file:

```text
scss/main.scss -> css/style.css
```

Build CSS once:

```bash
npm run sass:build
```

Watch SCSS changes:

```bash
npm run sass
```
